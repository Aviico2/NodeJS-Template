<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Profiles</title>
    <style>
      h1{font-size: 60px; text-transform: uppercase; letter-spacing: 2px; text-align: center;}
      p{font-size: 16px; text-align: center;}
      /* Form */
      #contact-form{ border-radius: 5px; background-color: #f2f2f2; padding: 20px;}
      input[type=submit] { background-color: #000000; color: white; padding: 10px 20px; border: none; border-radius: 3px; cursor: pointer; font-size: 16px;}
      input[type=text], [type=email], select, textarea { width: 100%; padding: 12px; border: 1px solid #ccc; border-radius: 4px; box-sizing: border-box; margin-top: 6px; margin-bottom: 16px; resize: vertical;}
      label{ width: 100%; color: #000000;}
    </style>
  </head>
  <body>
    <% include partials/nav.ejs %>
    <h1>Contant Us</h1>
    <p>Contant for try Node JS Post Method</p>
    <form id="contact-form" method="POST" action="/contact">
      <label for="who">Who do you want to contact?</label>
      <input type="text" name="who" value="">
      <label for="department">Which Department</label>
      <input type="text" name="department" value="">
      <label for="email">Your Email</label>
      <input type="email" name="email"/>
      <input type="submit" value="Submit"/>
    </form>
    <% include partials/footer.ejs %>
  </body>
</html>
